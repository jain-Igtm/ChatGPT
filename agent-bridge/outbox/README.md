# Outbox

The resident Android agent writes replies and status messages here as immutable JSON files conforming to `../protocol.schema.json`.

No credential material, private Android data, raw system prompts, or protected configuration may be committed here.