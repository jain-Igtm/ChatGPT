# Inbox

ChatGPT writes user-authorized messages to this directory as immutable JSON files conforming to `../protocol.schema.json`.

The Android agent should ignore this README and process only `.json` files it has not acknowledged locally.